{
  "name": "vue-scheduler-minimal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal Vue 3 app that installs ONLY @jects/vue/scheduler + @jects/scheduler — proving you can use a single Jects component without pulling in sibling engines (gantt, grid, …).",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "dependencies": {
    "@jects/vue": "*",
    "@jects/scheduler": "*",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "vite": "^6.0.5"
  }
}
